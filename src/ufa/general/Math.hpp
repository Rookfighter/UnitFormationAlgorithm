#ifndef UFA_MATH_HPP
#define UFA_MATH_HPP

#define MIN(a,b) ((a) < (b) ? (a) : (b))
#define MAX(a,b) ((a) > (b) ? (a) : (b))

#define MSEC_PER_SEC 1000
#define USEC_PER_SEC 1000000

#define FPS_TO_USEC(fps) ((USEC_PER_SEC) / (fps))

namespace ufa
{
	
}

#endif